# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
env/
venv/
lib/
!webapp/src/lib/
log/
htmlcov/
.tox/
.coverage
.coverage.*
.hypothesis/
.pytest_cache/
pip-log.txt
pip-delete-this-arg.txt

# Jupyter Notebook
.ipynb_checkpoints

# C extensions
*.so
*.lo
*.lo
*.la
*.blt
*.fpic
*.x86_64-linux-gnu.so

# Editor directories and files
.idea/                  # JetBrains IDEs (PyCharm, IntelliJ, WebStorm)
*.iml
.vscode/                # VS Code
*.suo
*.ntvs*
*.njsproj
*.sln
*.mfpak
.DS_Store               # macOS
Thumbs.db               # Windows
``
# React with Vite
/node_modules
/dist
.env
.env.*
!.env.example

# Local-only scripts
personal.cmd.sh
pipeline/perso-cmd.sh
workers/repopulate_facilities_clean.py
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.parcel-cache
.cache/
build/

# Local development automation
Makefile
layer_pkg/
dbt_layer_pkg/
pipeline/functions/dbt-runner/logs/
.aws-sam/

# Local audit / handoff notes
HANDOFF.md

# Local test/tooling artifacts
pipeline/response.json
/response.json
test.rest
skills-lock.json

# playwright-cli skill scratch output
.playwright-cli/


artifacts/
graphify-out/

# SNOMED CT RF2 data — licensed content, symlink to /databank, never commit
# (unanchored **/ form: the plain "assets/snomedct" pattern only matched the
# repo-root copy per git's path-anchoring rules, missing backend/assets/snomedct)
**/assets/snomedct

# eval-env seed data — never commit exported PII-adjacent snapshots or test credentials
backend/scripts/eval_seed/exports/
backend/scripts/eval_seed/*_accounts.json

# Phase B DeepEval — generated transcripts and scoring results, regenerate via the scripts
backend/scripts/triage_deepeval/transcripts/
backend/scripts/triage_deepeval/results/
backend/.deepeval/

# Sprint 17 Task 6 load-script output — regenerate via the scripts
backend/scripts/routing_shadow_eval/results/
backend/scripts/cache_load_eval/results/

# Git worktree scratch space
.worktrees/

# Task 6.3 GraphRAG eval harness (v1 vs v2) — generated transcripts and
# scoring results, regenerate via the scripts
backend/scripts/graphrag_eval/transcripts/
backend/scripts/graphrag_eval/results/

# Sprint 19 anchor_mapping.py build process artifacts — regenerate via
# anchor_resolver.py + the batch process documented in the SDD ledger;
# anchor_mapping.py itself (committed) is the source of truth going forward
backend/scripts/snomed_ingest/anchor_resolution.json
backend/scripts/snomed_ingest/anchor_mapping_merged.json
backend/scripts/snomed_ingest/anchor_batches/

# Task 4 depth_flagging.py output -- intermediate, regeneratable build artifact,
# regenerate via depth_flagging.py against the live graph
backend/scripts/snomed_ingest/depth_flagging_report.json
